# Release Evidence

This directory contains the structured evidence model for RetailOS releases.

Use `evidence.schema.json` for future JSON evidence records instead of creating a new free-form markdown file for every deployment. Existing historical markdown reports remain valid historical evidence, but new release automation should converge on structured records.
