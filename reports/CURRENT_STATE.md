Project: RetailOS
Active Phase: Phase 0.5 — Integration Hub MVP
Current Mode: Phase 0.5 Milestone 5 — Import API route deployed but environment-blocked
Build Status: Integration Hub schema/RLS foundation is implemented, merged, applied to hosted Supabase, and verified. Integration Hub setup UI is merged. RetailOS Import API boundary is merged. Import API credential/control-plane foundation is merged, applied to hosted Supabase, and verified. `/api/import/v1/records` is merged in PR #16 and deployed on Vercel `main`, but the production deployment currently returns 500 because required production Vercel environment variables are missing. Vercel runtime logs on 2026-07-13 showed `Missing required public environment variable: NEXT_PUBLIC_SUPABASE_URL`. Real connector authentication, provider sync workers, scheduled workers, and pipeline normalization from external records are not implemented yet.
Next Required Step: Configure required Vercel production/preview environment variables, redeploy `main`, then smoke test `/api/import/v1/records` with a tenant-scoped Import API credential.
